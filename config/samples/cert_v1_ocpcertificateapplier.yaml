apiVersion: cert.compute.io/v1
kind: OCPCertificateApplier
metadata:
  labels:
    app.kubernetes.io/name: cert-renewer-operator
    app.kubernetes.io/managed-by: kustomize
  name: ocpcertificateapplier-sample
spec:
  # TODO(user): Add fields here
